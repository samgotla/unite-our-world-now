- if can? :vote, Post, current_user
  .row
    %a.vote.btn.btn-sm.btn-default{ href: forum_post_vote_path(forum, post, :up),
                                   class: post.vote_class(current_user, :up) }
      %span.glyphicon.glyphicon-arrow-up

  .score= post.score

  .row
    %a.vote.btn.btn-sm.btn-default{ href: forum_post_vote_path(forum, post, :down),
                                   class: post.vote_class(current_user, :down) }
      %span.glyphicon.glyphicon-arrow-down
